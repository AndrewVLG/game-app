import { Box } from '@mui/material'

import {  MenuButton } from '../../UI'

export const Header = () => {
  return (
    <Box sx={{height: '50px', width: 'auto',}}>
      <MenuButton />
    </Box>
  )
}
